//Provide the implementation for the LinkedList class in this file.LinkedL

template<class T>
LinkedList<T>::LinkedList(){
//Node<T> *start = nullptr,*newl=nullptr,*end=nullptr;
//T takedata;
		//create memory for new node
		//1newl = new node;
		//new->data = takedata;
		//newl->next=nullptr;
		//new->prev=nullptr;
		//check for first node
	head = nullptr;
}
template>class T>
T LinkedList<T>remove(int index){
}

template<class T>
LinkedList<T>::LinkedList(const LinkedList<T>& other){
Node<T> *newNode = new Node;
Node *current;
	if(other.head == nullptr){
		first = nullptr; 
	}else{
		current = other.head;
		head->data = current->
	}

}


